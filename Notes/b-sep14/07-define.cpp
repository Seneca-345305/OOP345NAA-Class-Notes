#include <iostream>
#define sum(x,y) ((x)+(y))
int sumfunc(int x, int y) {
   return x + y;
}
using namespace std;
class Employee;
int main() {
   cout << "OOP345 Sep14th" << endl;
   int a = 10, b = 20;
   Employee I, J, K;
   int c;
   K = sum(I, J);
   c = sum(a, b) * 2;
   c = sum(a, b) * 2;
   c = sum(a, b) * 2;
   c = sum(a, b) * 2;
   c = sum(a, b) * 2;
   c = sum(a, b) * 2;
   c = sumfunc(a, b) + 2;
   c = sumfunc(a, b) + 2;a
   c = sumfunc(a, b) + 2;
   c = sumfunc(a, b) + 2;
   c = sumfunc(a, b) + 2;
   cout << c << endl;
   return 0;
}